// Passport.js OAuth configuration — populated in Phase 5 (OAuth)
// Strategies: passport-github2, passport-google-oauth20
const passport = require('passport');

// Strategy registrations will be added in Phase 5.
// This file is imported in app.js to initialize passport middleware.

module.exports = passport;
